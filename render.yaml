services:
  - type: web
    name: task-manager
    env: node
    plan: free  # Choose a paid plan if you need more resources
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        sync: false  # You will set this value in the Render dashboard
    healthCheckPath: /
